# @ratio/builder-registry

Section registry: first-party section types, typed editor settings, and island registration.

Part of the Ratio 3.0 monorepo — layout + naming rule in `docs/adr/0001-monorepo-layout.md`.
