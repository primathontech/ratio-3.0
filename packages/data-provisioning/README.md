# @ratio/data-provisioning

onboardStore / deleteStore — a tenant is just rows (tenant + domain + home route + membership), created atomically.

Part of the Ratio 3.0 monorepo — layout + naming rule in `docs/adr/0001-monorepo-layout.md`.
