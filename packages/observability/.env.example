# @ratio/observability is a library — it has no environment of its own and never reads process.env.
# The consuming app passes config to createLogger({ service, level }); e.g. the app supplies
# process.env.LOG_LEVEL for `level`. See that app's .env.example.
