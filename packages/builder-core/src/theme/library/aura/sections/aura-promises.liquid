<section class="rt"><div class="promises">
  <div class="promise"><b>{{ p1_t | default: 'Clean' | escape }}</b><span>{{ p1_s | default: 'Skin-kind formulas' | escape }}</span></div>
  <div class="promise"><b>{{ p2_t | default: 'Cruelty-free' | escape }}</b><span>{{ p2_s | default: 'Never tested on animals' | escape }}</span></div>
  <div class="promise"><b>{{ p3_t | default: 'Free samples' | escape }}</b><span>{{ p3_s | default: 'With every order' | escape }}</span></div>
  <div class="promise"><b>{{ p4_t | default: 'Easy returns' | escape }}</b><span>{{ p4_s | default: '30-day promise' | escape }}</span></div>
</div></section>
